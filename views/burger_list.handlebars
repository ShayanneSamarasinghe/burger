
    <div class ="row">
        <div class ="col">
            <!-- available -->
            {{#each available}}
            <div class="btn-toolbar mb-3">
                <div class="input-group-text" id="btnGroupAddon">{{name}}</div>
                <div>
                <button type="button" class="btn btn-secondary">devour {{id}}</button>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
    <div class ="col">
        {{!-- devoured burgers --}}
        {{#each devoured}}
        <div class="alert alert-secondary">
            {{name}}
        </div>   
        {{/each}} 
    </div>